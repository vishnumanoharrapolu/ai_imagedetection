{
  "name": "ai-image-detection",
  "version": "1.0.0",
  "description": "AI Image Detection Pro",
  "scripts": {
  "build:css": "npx tailwindcss -i input.css -o output.css --minify",
  "watch:css": "npx tailwindcss -i input.css -o output.css --watch"
},
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
